1. qpos와 action의 차이 : 명령을 내려서 움직인 위치를 읽어오면 다른 값임
2. action을 강제로 주는 경우 : 프레임1초로 했을 때 초기 데이터의 오류 가능성 제시 (하지만 0.1로 되어 무시되는 것으로 확인)
3. qpos를 어떻게 읽을 것인가 처음에 읽고 명령할지, 명령 후에 읽을 지

명령을 더 잘게 나눠서 시도하다 학습 데이터 값을 강제로 때렸을 때 에러나는 것을 확인
모터와 그리퍼가 동시에 들어가면서 모터에 혼선이 생김
